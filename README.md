# Snake
C++ Project

Since I am a noob:
cmake --preset=(default || Relelase)
cmake --build build/(Debug || Release)
cmake .

TODO: Can I work with vcpkg again? Our current setup is not ideal for adding packages.